# ABAC : Attribute Based Access Control  
  Databricks has securable Data Objects (catalog, database, table and so on). Unity Catalog provides centralized data governance of these data objects.
  We have created tags at owner level to secure data objects. The scope of these tags are valid across workspaces, catalogs, databases, tables and so on.
   
